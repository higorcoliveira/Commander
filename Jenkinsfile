pipeline {
  agent any
  
  stages {
    stage('Console Log') {
      steps {
        echo "TESTANDO JENKINSFILE"
      }
    }
    stage('Dir') {
      steps {
        echo "LISTANDO ARQUIVOS"
        sh "ls -la"
      }
    }
  }
}
